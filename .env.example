# Google OAuth (nail-habit dedicated credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# Production: your app domain (e.g. https://nailhabit.app)
# Dev: http://localhost:5173
# Desktop (Electron): http://localhost:3000 (the app loads from the embedded Express server)
APP_URL=http://localhost:5173

PORT=3000

# PayPal subscription plan IDs (from PayPal Developer Dashboard)
VITE_PAYPAL_CLIENT_ID=
VITE_PAYPAL_PLAN_ID_MONTHLY=
VITE_PAYPAL_PLAN_ID_YEARLY=

