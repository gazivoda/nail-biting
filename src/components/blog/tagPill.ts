// One neutral pill for every blog category, shared by the index and posts.
// There used to be eleven category colours on two separate maps: a rainbow on
// pages whose system has one accent, and two copies that could drift.
export const TAG_PILL =
  'inline-flex items-center rounded-full border border-[color:var(--sg-rule)] bg-white px-2.5 py-0.5 text-[0.8125rem] font-semibold text-[color:var(--sg-ink-2)]';
