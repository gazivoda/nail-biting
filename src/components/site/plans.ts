// What every plan includes, in one place: the pricing cards (PricingSection,
// on the homepage and /pricing) and the in-app paywall both read this, so the
// two can never describe the same subscription in different words.
export const PLAN_FEATURES = [
  'Detection for as long as you run it',
  'Current and best streak',
  'Full history with trigger tags',
  'Sound, flash, or both',
];

export const YEARLY_EXTRA = 'Priority support';
