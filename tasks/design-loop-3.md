# Design loop, round 3 (5 iterations, every 5 min)

iterations: 1

## Backlog
- 2: Paywall matches /pricing: Yearly first at every width, drop tinted icon tiles, check icons in trust row, header links text-sm + 44px targets, trial days left near the top, one 'billed yearly', h1 on success screen.
- 3: Onboarding tour as a real dialog: focus into it, Escape closes, aria-labelledby, 'Next'/'Done' with line icon, 'Skip tour' target size.
- 4: One cancel answer ('in Settings') on /pricing, paywall, FAQ; app sidebar uses the site's arch logo.
- 5: Watch says 'on-device' three times (drop the header pill); final short review.
Owner: PrivacyPage section 7 says incident logs are stored on the server; the code keeps them in the browser. Legal text, owner to decide.

## Log
- 1/5: /about + crawler mirror + one blog post: no false 'no analytics'/'no account' claims; em dashes removed
