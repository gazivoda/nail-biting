# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# MediaPipe bundled model files (large binaries, downloaded at build time)
public/mediapipe-models/

# Server data (users DB — never commit)
data/users.json

# Server secrets
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Desktop app installers (large binaries — upload to server manually or via volume)
downloads/*.dmg
downloads/*.exe
downloads/*.zip
downloads/*.deb
downloads/*.AppImage
