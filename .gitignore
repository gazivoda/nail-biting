# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local


# Server data (users DB — never commit)
data/users.db
data/users.db-wal
data/users.db-shm
data/users.json
# Last-submitted <lastmod> per URL, written by scripts/indexnow-ping.mjs.
data/indexnow-state.json

# Server secrets
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Desktop app installers — not served by the web app, keep out of git
downloads/

# impeccable: keep the design record (design.json, surfaces/, critique/), not the
# working files (screenshots, decision-page payloads, question logs)
.impeccable/review/
.impeccable/decision/
.impeccable/questions/
.impeccable/mocks/
.impeccable/build/
.impeccable/config.local.json
